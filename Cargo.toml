[package]
name = "itch-io"
version = "0.0.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[[bin]]
name = "itch-io"
path = "src/main.rs"
required-features = [ "cli" ]

[dependencies]
once_cell = "1.15.0"
reqwest = { version = "0.11.12", default-features = false, features = [ "json", "cookies" ] }
scraper = { version = "0.13.0", default-features = false }
serde = { version = "1.0.147", features = ["derive"] }
thiserror = "1.0.37"
tokio = { version = "1.21.2", features = [ "rt" ] }
url = { version = "2.3.1", features = [ "serde" ] }

# Optional
anyhow = { version = "1.0.66", optional = true }
argh = { version = "0.1.9", optional = true }
itoa = { version = "1.0.4", optional = true }

[dev-dependencies]
tokio = { version = "1.21.2", features = [ "macros" ]  }

[features]
default = [
    "rustls-tls",
]

native-tls = [ "reqwest/native-tls" ]
rustls-tls = [ "reqwest/rustls-tls" ]

cli = [
    "anyhow",
    "argh",
    "itoa",
    "tokio/rt-multi-thread",
]
